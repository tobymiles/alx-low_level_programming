#include <stdio.h>

/**
 * main - function prints all strings from 0 to 9
 *
 * Return: Always o
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	printf("\n");

	return (0);
}
